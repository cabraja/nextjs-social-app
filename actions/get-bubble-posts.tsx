import db from "@/lib/db";

export default async function getBubblePosts(id: string) {
  try {
    // const posts = await db.post.findMany({
    //   where: {
    //     bubbleId: id,
    //     bubble:{
    //         accessType:
    //     }
    //   },
    // });
  } catch (error) {}
}
